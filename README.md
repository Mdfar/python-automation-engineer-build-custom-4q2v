Staqlt AI Video Factory

Internal tool for automated TikTok/Reels production.

Features

Scripting: LLM-powered viral hook generation.

Dual-Path: Avatar animation via Veo + ElevenLabs Speech-to-Speech.

B-Roll: Imagen 3 image generation → Veo motion.

Assembly: MoviePy orchestration for cuts, transitions, and subs.

Setup

Configure config.yaml with your API credentials.

Run pip install -r requirements.txt.

Launch UI: streamlit run app.py.